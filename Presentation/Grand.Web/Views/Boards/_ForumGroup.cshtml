@model ForumGroupModel
@using Grand.Web.Models.Boards;
<div class="forums-table-section forum-group">
        <h5>@Html.RouteLink(Model.Name, "ForumGroupSlug", new { id = Model.Id, slug = Model.SeName })</h5>
    <div class="forums-table-section-body">
            <table class="table table-hover">
                <colgroup>
                    <col />
                </colgroup>
                <thead class="table-inverse">
                    <tr>
                        <th>
                            <div class="col-12">
                                <div class="row">
                                    <div class="forum-details offset-md-1 col-md-5">
                                        @T("Forum.Forum")
                                    </div>
                                    <div class="topics col-md-1 hidden-sm-down">
                                        @T("Forum.Topics")
                                    </div>
                                    <div class="posts col-md-1 hidden-sm-down">
                                        @T("Forum.Posts")
                                    </div>
                                    <div class="latest-post col-md-3 hidden-sm-down">
                                        @T("Forum.LatestPost")
                                    </div>
                                </div>
                            </div>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var forum in Model.Forums)
                    {
                        <tr>
                            <td>
                                <div class="col-12">
                                    <div class="row">
                                        <div class="post col-12 col-md-1">
                                            <i class="fa fa-envelope-o" aria-hidden="true"></i>
                                        </div>
                                        <div class="forum-details col-12 col-md-6">
                                            <div class="forum-title">
                                                @Html.RouteLink(forum.Name, "ForumSlug", new { id = forum.Id, slug = forum.SeName })
                                            </div>
                                            <div class="forum-description">
                                                @forum.Description
                                            </div>
                                        </div>
                                        <div class="topics col-12 col-md-1 hidden-sm-down">@forum.NumTopics</div>
                                        <div class="posts col-12 col-md-1 hidden-sm-down">@forum.NumPosts</div>
                                        <div class="latest-post col-12 col-md-3">
                                            @Html.Action("LastPost", new { forumPostId = forum.LastPostId, showTopic = true })
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>            
                    }
                </tbody>
            </table>
        </div>
    </div>
