@model ManufacturerModel
@{
    //page title
    ViewBag.Title = T("Admin.Catalog.Manufacturers.EditManufacturerDetails").Text;
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "manufacturer-form" }))
{
    @Html.AntiForgeryToken()
    <div class="row">
        <div class="col-md-12">
            <div class="portlet light form-fit">
                <div class="portlet-title">
                    <div class="caption">
                        <i class="fa fa-barcode"></i>
                        @T("Admin.Catalog.Manufacturers.EditManufacturerDetails") - @Model.Name 
                        <small>@Html.ActionLink("(" + T("Admin.Catalog.Manufacturers.BackToList") + ")", "List")</small>
                    </div>
                    <div class="actions">
                        <div class="btn-group btn-group-devided">
                            <input type="submit" value="@T("Admin.Common.Preview")" onclick="javascript:OpenWindow('@Url.RouteUrl("Manufacturer", new { SeName = Model.SeName })', 800, 600, true); return false;" class="btn green" />
                            <input type="submit" name="save" class="btn default" value="@T("Admin.Common.Save")" />
                            <input type="submit" name="save-continue" class="btn default" value="@T("Admin.Common.SaveContinue")" />
                            <span id="manufacturer-delete" class="btn red">@T("Admin.Common.Delete")</span>
                        </div>
                    </div>
                </div>
                <div class="portlet-body form">
                    @Html.Partial("_CreateOrUpdate", Model)
                </div>
            </div>
        </div>
    </div>


}
@Html.DeleteConfirmation("manufacturer-delete")