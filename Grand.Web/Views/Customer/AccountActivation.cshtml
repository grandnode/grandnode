@model AccountActivationModel
@using Grand.Web.Models.Customer;
@{
    Layout = "_ColumnsOne";

    //title
    Html.AddTitleParts(T("PageTitle.AccountActivation").Text);
}
<div class="page account-activation-page">
    <h1 class="h2 generalTitle">@T("Account.AccountActivation")</h1>
    <div class="result">
        <div class="result card bg-info p-3 text-white">@Model.Result</div>
    </div>
</div>
