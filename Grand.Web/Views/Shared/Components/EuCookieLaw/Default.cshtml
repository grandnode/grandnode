<script asp-location="Footer">
    $(document).ready(function () {
        $('#eu-cookie-bar-notification').show();

        $('.cookielaw-button').click(function () {
            var postData = {
                accept: $(this).attr("data-accept")
            };
            $.ajax({
                cache: false,
                type: 'POST',
                data: postData,
                url: '@Url.RouteUrl("EuCookieLawAccept")',
                dataType: 'json',
                success: function (data) {
                    $('#eu-cookie-bar-notification').hide();
                },
                failure: function () {
                    alert('Cannot store value');
                }
            });
        });
    });
</script>

<div id="eu-cookie-bar-notification" class="eu-cookie-bar-notification" style="display:none;">
    <div class="buttons-more">
        <blockquote class="card-bodyquote d-inline-flex justify-content-center align-items-center flex-wrap flex-md-nowrap w-100 mb-0">
            <p class="mb-0 text-white text-md-left text-center">@T("EUCookieLaw.Description2")</p>
            <div class="btn-group mx-0 mx-md-3 mt-3 mt-md-0">
                <a class="cookielaw-button btn-sm btn btn-light text-dark px-1 px-sm-2" id="eu-cookie-ok" data-accept="true">@T("EUCookieLaw.Accept")</a>
                <a class="cookielaw-button btn-sm btn btn-light text-dark px-1 px-sm-2 mx-sm-1 mx-0" id="eu-cookie-refuse" data-accept="false">@T("EUCookieLaw.Refuse")</a>
                <a class="btn btn-sm btn-info learn-more px-1 px-sm-2" href="@Url.RouteUrl("Topic", new { SeName = TopicSeNameConstants.Privacyinfo })">@T("EUCookieLaw.LearnMore")</a>
            </div>
        </blockquote>
    </div>
</div>
